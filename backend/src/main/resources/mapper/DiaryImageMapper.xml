<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.infreeJ.backend.repository.DiaryImageMapper">

	<insert id="diaryImageInsert" parameterType="diaryImageDto">
		INSERT INTO diary_images(id, diary_id, org_diary_img_name, uuid_diary_img_name)
		VALUES(diary_images_seq.NEXTVAL, #{id}, #{orgDiaryImgName}, #{uuidDiaryImgName})
	</insert>
	
	<update id="diaryImageUpdate" parameterType="diaryImageDto">
		UPDATE diary_images
		SET org_diary_img_name = #{orgDiaryImgName}, uuid_diary_img_name = #{uuidDiaryImgName}
		WHERE id = #{id}
	</update>
	
	<delete id="diaryImageDelete" parameterType="long">
		DELETE FROM diary_images
		WHERE id = #{id}
	</delete>

</mapper>